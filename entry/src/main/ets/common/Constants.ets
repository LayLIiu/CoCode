/**
 * 常量定义
 */
export class Constants {
  // Tab标签名称
  static readonly TAB_CLAUDE: string = 'Claude'
  static readonly TAB_SETTINGS: string = '设置'

  // 页面标题
  static readonly TITLE_CLAUDE: string = 'Claude Code'
}

/**
 * Tab索引枚举
 */
export enum TabIndex {
  CLAUDE = 0,
  SETTINGS = 1
}
