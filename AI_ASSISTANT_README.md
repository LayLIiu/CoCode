# AI编程助手App - 鸿蒙版

## 项目说明

这是一个基于ArkTS开发的iOS风格深色模式AI编程助手应用，完全适配鸿蒙系统。项目1:1还原了iOS原生UI设计，包括深色主题、圆角卡片、毛玻璃效果等视觉元素，以及平滑的动画交互。

## 功能特性

### 已实现功能

1. **会话列表页**
   - 顶部导航栏：标题、历史图标、新建会话按钮
   - 搜索栏：支持实时搜索过滤
   - 会话列表：带状态图标的会话卡片
   - 底部Tab栏：4个标签切换（Claude、CodeX、Open Code、设置）

2. **对话详情页**
   - 顶部导航栏：返回按钮、会话标题、功能菜单按钮
   - 状态条：显示工具执行状态，带蓝色圆点指示器
   - 消息列表：支持多种消息类型（文本、工具执行、执行中状态）
   - 底部输入区域：输入框、加号按钮、发送按钮

3. **设置页面**
   - 深色模式设置
   - 通知设置
   - 自动保存设置

### 设计规范

- **深色主题**：纯黑背景(#000000)，深灰卡片(#1C1C1E)
- **颜色系统**：主文字白色、次级文字灰色、强调色（蓝、紫、橙、绿）
- **圆角规范**：小(8px)、中(12px)、大(16px)、超大(20px)
- **间距规范**：6个级别的间距系统
- **字号规范**：标题(28px)、标题(18px)、正文(16px)、说明(14px)、微小(12px)

### 动画效果

- Tab切换淡入淡出动画
- 发送按钮颜色变化动画
- 列表滚动弹簧效果
- 点击反馈动画

## 项目结构

```
entry/src/main/
├── ets/
│   ├── common/                    # 公共配置
│   │   ├── Theme.ets             # 主题样式
│   │   └── Constants.ets         # 常量定义
│   ├── components/               # 可复用组件
│   │   ├── TabBar.ets           # 底部导航栏
│   │   ├── ConversationItem.ets  # 会话列表项
│   │   ├── SearchBar.ets        # 搜索栏
│   │   ├── MessageItem.ets      # 消息项
│   │   ├── ChatNavigationBar.ets # 对话导航栏
│   │   └── InputArea.ets        # 输入区域
│   ├── pages/                    # 页面
│   │   ├── Index.ets            # 入口页面
│   │   ├── MainPage.ets         # 主页面
│   │   ├── ConversationListPage.ets # 会话列表
│   │   ├── ChatDetailPage.ets   # 对话详情
│   │   └── SettingsPage.ets     # 设置页面
│   └── viewmodel/                # 数据模型
│       ├── ConversationModel.ets # 会话模型
│       ├── MessageModel.ets     # 消息模型
│       ├── MockData.ets         # 模拟数据
│       └── MockMessageData.ets  # 消息数据
└── resources/
    └── base/
        ├── media/                # 图标资源（20个SVG图标）
        ├── element/              # 颜色、字符串、浮点数资源
        └── profile/              # 配置文件
            └── main_pages.json    # 页面路由配置
```

## 使用方法

### 1. 打开项目

1. 在DevEco Studio中打开项目：`/Users/liujie/DevEcoStudioProjects/MyApplication2`
2. 等待项目同步完成

### 2. 运行项目

1. 连接鸿蒙设备或启动模拟器
2. 点击运行按钮（绿色三角形）
3. 等待编译和安装完成

### 3. 功能测试

#### 会话列表
- 查看不同Tab下的会话列表（Claude、CodeX、Open Code）
- 点击搜索框搜索会话
- 点击右上角加号按钮（显示日志）

#### 对话详情
- 点击任意会话卡片进入对话详情页
- 查看不同类型的消息（文本、工具执行、执行中状态）
- 在底部输入框输入文字并发送
- 点击返回按钮返回会话列表

#### 设置页面
- 切换到设置Tab
- 切换各种开关选项

## 核心组件说明

### 1. TabBar组件
底部导航栏组件，支持：
- 毛玻璃效果
- 选中态高亮
- 平滑切换动画

### 2. ConversationItem组件
会话列表项组件，包含：
- 状态图标（支持休眠/激活状态）
- 会话标题
- 文件路径
- 消息数
- 时间戳
- 点击跳转到对话详情页

### 3. MessageItem组件
消息项组件，支持三种消息类型：
- 文本消息：黑色圆角气泡，白色文字
- 工具执行消息：显示工具图标、名称、执行状态
- 执行中状态：橙色图标、加载动画、执行时长

### 4. InputArea组件
底部输入区域组件，支持：
- 文本输入
- 发送消息
- 添加附件

## 技术特点

- **完全使用ArkTS原生组件**，适配鸿蒙系统
- **响应式布局**，使用相对尺寸和权重布局
- **模块化设计**，组件可复用，易于维护
- **完整的数据模型**，包含消息类型、工具状态等
- **页面导航**，支持会话列表到详情页的跳转
- **iOS风格设计**，完美复刻深色模式UI

## 扩展建议

1. **网络请求**：集成HTTP客户端，实现真实数据加载
2. **数据持久化**：使用Preferences或数据库存储会话数据
3. **实时通信**：集成WebSocket实现实时消息推送
4. **富文本支持**：支持Markdown渲染、代码高亮
5. **多媒体消息**：支持图片、文件、语音等消息类型
6. **消息搜索**：添加消息搜索功能
7. **主题切换**：扩展支持浅色模式
8. **多语言**：添加国际化支持

## 注意事项

1. 所有SVG图标已放置在 `resources/base/media/` 目录
2. 页面路由配置在 `resources/base/profile/main_pages.json`
3. 所有组件遵循鸿蒙ArkTS语法规范
4. 使用了路由导航（router）实现页面跳转

## 版本信息

- HarmonyOS SDK: API 12
- DevEco Studio: 最新版本
- ArkTS: 最新语法

## 许可证

MIT License
